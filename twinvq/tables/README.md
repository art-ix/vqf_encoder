# Codebook generator

`twinvq/src/twinvq_tables.cpp` is already generated and is what the build uses.

`gen_tables.py` is only needed if you want to regenerate those tables from the public TwinVQ codebook dumps (`twinvq_data.h` and `metasound_twinvq_data.h`). Those header dumps are not committed.
